# v0.4.3 handoff — state as of 2026-09-02

Branch: `worktree-v0.4.3-query-observability` (based on main @ cbe3970 = v0.4.2).
Plan: `docs/roadmap/v0.4.3-query-observability-plan.md` (read it first — it has
full task specs, code anchors, and test standards).

## Done, reviewed, merge-quality (commits on this branch)

| Task | Commits | State |
|---|---|---|
| T1 WHERE-equality → IndexScan fold | `3fde1ea` + fix `ecaf310` | Reviewed clean (1 fix round: full-row equivalence assertions) |
| T2 compound AND → IndexIntersect | `d1279b3` + fix `8f3e22f` | Reviewed clean (1 fix round: fallback compares in 3 tests + error-propagation made identical between indexed and fallback paths) |
| T3 subscription label-skip | `9dd799b` | Reviewed clean, zero fixes (all 23 WalRecord variants audited; default-deny catch-all) |

Test state at 9dd799b: full workspace green (~1380 tests), clippy clean both
feature sets, fmt clean.

## NOT done

- **T4 (/metrics + slow-query log): PARTIAL, UNREVIEWED.** Commit `9fbfa80`
  is a WIP snapshot taken after the implementing agent was interrupted —
  it has NOT been tested or reviewed and may not compile. Contents:
  core-api accessors (commit_seq, wal_size_bytes via core-storage/fs.rs),
  slow-query timing + ring buffer, a new `crates/core-api/tests/slow_query.rs`,
  server dep changes. Missing (probably): the actual `GET /metrics` route +
  handler in `crates/server/src/http.rs`, RSS helper, docs, full test pass.
  Either finish it against the plan's T4 spec, or `git revert 9fbfa80` and
  redo T4 from the plan.
- **Release task**: not started (plan's Release section: bump 0.4.2→0.4.3,
  CHANGELOG, gates, LOCAL tag, branch push, CI watch; never push the tag —
  publish needs the maintainer's PyPI env approvals, TWO per release).

## Known deferred minors (fold into T4-finish or release cleanup)

- `query_sub_exec_count()`/`reset_query_sub_exec_count()` (core-api) lack
  `#[doc(hidden)]`.
- T3's rule-delta test could add a defense-in-depth "no event delivered"
  assertion.
- v0.4.2 leftovers: T1 stress test leaves tmp dirs; tls.rs same.

## Process notes for whoever continues

- CI has 9 jobs incl. `format-compat` and `recall-gates` (the 5k recall test
  takes ~6 min). All must be green before merge.
- The binding quality bar this branch established: every index-path test
  asserts FULL row equality vs the unindexed fallback (not counts); the
  subscription skip is default-deny per WAL record type.
- Main branch: force-push/deletion blocked; merges via PR (no approvals
  required).
