---
description: Live code graph for this repo — map, context, impact, owners, why, recall, remember.
---

Invoke the `mushroom` skill (`/mushroom:mushroom`) with $ARGUMENTS as the turn's request, and follow it exactly. Do not answer without calling it first.
