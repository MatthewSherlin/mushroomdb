services:
  mushroomdb:
    build: .
    image: mushroomdb:local
    ports:
      - "8080:8080"
    volumes:
      - mushroomdb-data:/data
    # Image CMD binds 0.0.0.0:8080; non-loopback serve requires a token.
    environment:
      MUSHROOMDB_TOKEN: changeme

volumes:
  mushroomdb-data:
