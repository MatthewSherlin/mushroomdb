[package]
name = "core-api"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
core-storage = { path = "../core-storage" }
core-rules = { path = "../core-rules" }
core-query = { path = "../core-query" }
bincode = { workspace = true }
