[package]
name = "sim-harness"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
core-api = { path = "../core-api" }
core-storage = { path = "../core-storage" }

[dev-dependencies]
proptest = { workspace = true }
